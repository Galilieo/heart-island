# 心屿 AI 情绪记录与匿名互助社区系统

# 08-测试说明书

## 1. 文档目的

本文档用于说明心屿 AI 情绪记录与匿名互助社区系统的测试方案，包括测试目标、测试范围、测试环境、测试方法、功能测试用例、接口测试用例、权限测试、异常测试、前后端联调测试和验收标准。

测试设计以产品功能是否可用为核心，同时结合软件工程中的边界条件、异常路径、权限控制和数据安全要求，保证系统能够稳定支撑用户注册登录、心情记录、AI 回复、匿名社区、后台管理和数据统计等功能。

## 2. 测试目标

系统测试需要达到以下目标：

1. 验证用户注册、登录、退出登录流程是否正常。
2. 验证密码加密、token 生成、token 携带和 token 解析是否正常。
3. 验证心情记录新增、查询、修改、删除功能是否正常。
4. 验证用户只能访问自己的私有数据。
5. 验证 AI 回复生成和保存流程是否正常。
6. 验证匿名社区帖子、评论、点赞、收藏功能是否正常。
7. 验证管理员后台管理和内容审核功能是否正常。
8. 验证统计接口返回数据是否准确。
9. 验证前端页面和后端接口联调是否顺畅。
10. 验证异常场景下系统能返回明确提示。

## 3. 测试范围

### 3.1 当前阶段测试范围

当前最小闭环阶段重点测试：

1. 用户注册。
2. 用户登录。
3. JWT token 保存和携带。
4. 密码加密。
5. 新增心情记录。
6. 查询心情记录列表。
7. 删除心情记录。
8. 退出登录。
9. 统一 Result 返回。
10. 全局异常处理。
11. CORS 跨域。
12. AI 回复生成、展示和保存。
13. AI 调用失败兜底。
14. AI 调用记录写入 ai_reply 表。
15. 首页 message、loading、empty 状态。
16. 登录路由守卫。
17. 最近 7 天心情趋势。

### 3.2 后续完整系统测试范围

后续扩展阶段继续测试：

1. 心情记录详情。
2. 心情记录修改。
3. AI 回复。
4. 匿名帖子发布。
5. 帖子列表和详情。
6. 评论。
7. 点赞和取消点赞。
8. 收藏和取消收藏。
9. 管理员后台。
10. 用户管理。
11. 帖子审核。
12. 评论管理。
13. 话题管理。
14. 数据统计。

## 4. 测试环境

### 4.1 后端环境

| 项目 | 内容 |
| --- | --- |
| 开发语言 | Java 17 |
| 框架 | Spring Boot |
| ORM | MyBatis-Plus |
| 数据库 | MySQL |
| 构建工具 | Maven |
| 后端地址 | http://localhost:8080 |

### 4.2 前端环境

| 项目 | 内容 |
| --- | --- |
| 框架 | Vue3 |
| 构建工具 | Vite |
| 请求工具 | axios / request.js |
| 前端地址 | http://localhost:5173 |
| 浏览器 | Edge / Chrome |

### 4.3 测试工具

| 工具 | 用途 |
| --- | --- |
| Apifox | 接口测试 |
| 浏览器 DevTools | 查看 Network、LocalStorage、Console |
| MySQL 客户端 | 查看数据库数据 |
| IDEA 控制台 | 查看后端日志和异常 |
| npm build | 检查前端构建是否通过 |

## 5. 测试方法

### 5.1 功能测试

通过页面操作验证系统功能是否符合需求。

示例：

1. 在注册页注册账号。
2. 在登录页登录账号。
3. 在首页新增心情记录。
4. 查看记录列表。
5. 删除记录。
6. 退出登录。

### 5.2 接口测试

通过 Apifox 调用后端接口，验证请求参数、响应结果和异常提示是否正确。

### 5.3 联调测试

通过前端页面触发请求，在浏览器 Network 中查看接口请求和响应，验证前后端联调是否正常。

### 5.4 权限测试

通过不携带 token、携带错误 token、普通用户访问管理员接口等方式，验证权限控制是否有效。

### 5.5 数据库验证

通过 MySQL 客户端查看数据库数据，验证注册、心情记录、帖子、评论、AI 回复等数据是否正确保存。

## 6. 用户注册测试

### 6.1 注册成功

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证用户可以正常注册 |
| 前置条件 | 用户名未被占用 |
| 操作步骤 | 输入 username、password、nickname，点击注册 |
| 预期结果 | 返回注册成功，数据库新增用户 |

请求示例：

```json
{
  "username": "test",
  "password": "123456",
  "nickname": "小屿"
}
```

预期响应：

```json
{
  "code": 200,
  "message": "注册成功",
  "data": true
}
```

### 6.2 用户名为空

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证用户名为空时注册失败 |
| 操作步骤 | username 为空，提交注册 |
| 预期结果 | 返回用户名不能为空 |

### 6.3 密码为空

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证密码为空时注册失败 |
| 操作步骤 | password 为空，提交注册 |
| 预期结果 | 返回密码不能为空 |

### 6.4 昵称为空

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证昵称为空时注册失败 |
| 操作步骤 | nickname 为空，提交注册 |
| 预期结果 | 返回昵称不能为空 |

### 6.5 用户名重复

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证重复用户名不能注册 |
| 操作步骤 | 使用已存在 username 注册 |
| 预期结果 | 返回用户名已存在 |

### 6.6 密码加密验证

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证数据库不保存明文密码 |
| 操作步骤 | 注册成功后查看 user 表 password 字段 |
| 预期结果 | password 为 BCrypt 密文，不是用户输入的明文 |

## 7. 用户登录测试

### 7.1 登录成功

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证账号密码正确时可以登录 |
| 前置条件 | 用户已注册 |
| 操作步骤 | 输入正确 username 和 password |
| 预期结果 | 返回登录成功、user 和 token |

预期响应：

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "user": {
      "id": 1,
      "username": "test",
      "nickname": "小屿"
    },
    "token": "xxxxx.yyyyy.zzzzz"
  }
}
```

### 7.2 密码错误

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证密码错误时不能登录 |
| 操作步骤 | 输入错误 password |
| 预期结果 | 返回账号或密码错误 |

### 7.3 用户不存在

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证不存在用户不能登录 |
| 操作步骤 | 输入不存在 username |
| 预期结果 | 返回账号或密码错误 |

### 7.4 登录返回不包含 password

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证登录接口不暴露密码 |
| 操作步骤 | 登录成功后查看响应 data.user |
| 预期结果 | user 中不包含 password 字段 |

### 7.5 本地存储验证

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证前端保存登录状态 |
| 操作步骤 | 登录成功后打开 DevTools -> Application -> LocalStorage |
| 预期结果 | 存在 loginUser 和 token |

## 8. token 测试

### 8.1 请求自动携带 token

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证 request.js 请求拦截器生效 |
| 操作步骤 | 登录后点击刷新心情记录，打开 Network 查看 /mood/list |
| 预期结果 | Request Headers 中存在 token |

### 8.2 未携带 token

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证未登录不能访问受保护接口 |
| 操作步骤 | 清除 localStorage 后请求心情记录接口 |
| 预期结果 | 返回请先登录 |

### 8.3 错误 token

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证错误 token 不能通过认证 |
| 操作步骤 | Apifox 请求头传入错误 token |
| 预期结果 | 返回登录失效或服务异常提示 |

## 9. 心情记录测试

### 9.1 新增心情记录成功

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证登录用户可以新增心情记录 |
| 前置条件 | 用户已登录 |
| 操作步骤 | 输入心情类型和内容，点击新增 |
| 预期结果 | 返回新增成功，列表刷新后显示新记录 |

### 9.2 心情类型为空

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证心情类型为空时新增失败 |
| 操作步骤 | moodType 为空，提交新增 |
| 预期结果 | 返回心情类型不能为空 |

### 9.3 心情内容为空

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证心情内容为空时新增失败 |
| 操作步骤 | content 为空，提交新增 |
| 预期结果 | 返回心情内容不能为空 |

### 9.4 查询心情记录列表

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证用户可以查询自己的心情记录 |
| 操作步骤 | 登录后请求心情记录列表 |
| 预期结果 | 返回当前用户的记录列表 |

### 9.5 不传 userId 查询记录

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证后端从 token 解析 userId |
| 操作步骤 | 前端请求 /mood/list，不传 userId |
| 预期结果 | 正常返回当前登录用户的记录 |

### 9.6 删除心情记录

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证用户可以删除心情记录 |
| 操作步骤 | 点击删除，确认后调用删除接口 |
| 预期结果 | 返回删除成功，列表不再显示该记录 |

### 9.7 删除前确认

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证删除危险操作有二次确认 |
| 操作步骤 | 点击删除按钮 |
| 预期结果 | 出现确认提示，取消后不删除 |

### 9.8 详情查看

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证用户可以查看自己的心情记录详情 |
| 操作步骤 | 点击心情记录卡片中的详情按钮 |
| 预期结果 | 打开详情弹窗，展示心情类型、内容、AI 回复和时间 |

### 9.9 修改心情记录

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证用户可以修改自己的心情记录 |
| 操作步骤 | 在详情弹窗点击编辑，修改内容后保存 |
| 预期结果 | 返回修改成功，列表和详情刷新，AI 回复重新生成 |

### 9.10 条件筛选和分页

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证分页、心情类型筛选和日期筛选正常 |
| 操作步骤 | 选择心情类型、开始日期、结束日期并点击查询 |
| 预期结果 | 返回符合条件的当前用户记录，分页按钮正常 |

## 10. AI 回复测试

### 10.1 生成 AI 回复成功

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证新增心情后可生成 AI 回复 |
| 前置条件 | AI 接口可用 |
| 操作步骤 | 新增心情记录并触发 AI 回复 |
| 预期结果 | 返回 AI 回复内容，mood_record.ai_reply 保存最新回复，ai_reply 表新增成功记录 |

### 10.2 AI 调用失败

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证 AI 调用失败不影响心情记录保存 |
| 操作步骤 | 模拟 AI 接口失败 |
| 预期结果 | 心情记录保存成功，AI 回复返回默认陪伴文案，ai_reply 表记录 status=0 和 error_message |

### 10.3 AI 回复前端展示

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证 AI 回复可以在前端展示 |
| 操作步骤 | 新增或修改心情记录后查看列表和详情 |
| 预期结果 | 心情卡片和详情弹窗中展示“心屿回复” |

### 10.4 AI 调用记录字段

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证 AI 调用记录字段保存完整 |
| 操作步骤 | 新增或修改心情记录后查看 ai_reply 表 |
| 预期结果 | model_name、prompt、reply_content、target_type、target_id、status、create_time 保存正确 |

## 11. 社区模块测试

### 11.1 发布帖子成功

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证登录用户可以发布帖子 |
| 操作步骤 | 填写话题、标题、内容、匿名选项并发布 |
| 预期结果 | 返回发布成功，帖子出现在列表 |

### 11.2 标题为空

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证标题为空时发布失败 |
| 操作步骤 | title 为空提交 |
| 预期结果 | 返回标题不能为空 |

### 11.3 内容为空

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证内容为空时发布失败 |
| 操作步骤 | content 为空提交 |
| 预期结果 | 返回内容不能为空 |

### 11.4 评论帖子

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证用户可以评论帖子 |
| 操作步骤 | 在帖子详情页输入评论并提交 |
| 预期结果 | 评论成功，评论列表新增该评论 |

### 11.5 点赞帖子

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证用户可以点赞帖子 |
| 操作步骤 | 点击点赞 |
| 预期结果 | 点赞成功，点赞数增加 |

### 11.6 重复点赞

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证不能重复点赞 |
| 操作步骤 | 对同一帖子重复点赞 |
| 预期结果 | 返回已点赞或保持点赞状态不重复增加 |

### 11.7 收藏帖子

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证用户可以收藏帖子 |
| 操作步骤 | 点击收藏 |
| 预期结果 | 收藏成功 |

### 11.8 重复收藏

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证不能重复收藏 |
| 操作步骤 | 对同一帖子重复收藏 |
| 预期结果 | 返回已收藏或保持收藏状态不重复新增 |

## 12. 管理端测试

### 12.1 普通用户访问管理端

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证普通用户不能访问管理员接口 |
| 操作步骤 | 使用普通用户 token 请求 /admin 接口 |
| 预期结果 | 返回权限不足 |

### 12.2 管理员查询用户列表

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证管理员可以查询用户列表 |
| 操作步骤 | 管理员登录后请求用户管理接口 |
| 预期结果 | 返回用户分页列表 |

### 12.3 禁用用户

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证管理员可以禁用用户 |
| 操作步骤 | 修改用户 status 为 0 |
| 预期结果 | 用户状态变为禁用 |

### 12.4 审核帖子

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证管理员可以审核帖子 |
| 操作步骤 | 修改帖子 status |
| 预期结果 | 帖子状态更新成功 |

### 12.5 删除评论

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证管理员可以删除违规评论 |
| 操作步骤 | 管理员删除评论 |
| 预期结果 | 评论状态更新或不再展示 |

## 13. 数据统计测试

### 13.1 最近 7 天心情趋势

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证用户端情绪趋势统计 |
| 操作步骤 | 创建最近 7 天内不同心情类型记录，调用 /mood/trend/recent7 |
| 预期结果 | 返回 total、mainMoodType、lastMoodType、lastRecordTime 和 moodCounts |

### 13.2 心情类型占比

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证心情类型占比统计 |
| 操作步骤 | 创建不同 moodType 记录，调用统计接口 |
| 预期结果 | 返回各心情类型数量或占比 |

### 13.3 平台概览统计

| 项目 | 内容 |
| --- | --- |
| 测试目标 | 验证管理端概览数据 |
| 操作步骤 | 管理员调用 /admin/stat/overview |
| 预期结果 | 返回用户数、帖子数、评论数、AI 调用次数等 |

## 14. 前端页面测试

### 14.1 登录页

测试点：

1. 输入框正常输入。
2. 空字段有提示。
3. 登录成功后跳转首页。
4. 登录失败展示错误提示。

### 14.2 注册页

测试点：

1. 注册成功后跳转登录页。
2. 用户名重复提示。
3. 空字段提示。

### 14.3 首页

测试点：

1. 页面展示当前用户昵称。
2. 新增心情记录成功。
3. 查询心情记录成功。
4. 删除心情记录成功。
5. 无记录时展示空状态。
6. 不同心情类型显示不同标签样式。
7. 时间格式展示正常。
8. 新增、查询、详情、删除、修改时有 loading 或明确反馈。
9. 删除前有确认提示。
10. 编辑中关闭详情会提示是否放弃修改。
11. 未登录访问 /home 会被路由守卫拦回登录页。
12. 最近 7 天心情趋势展示记录数、主要心情、最近一次心情和类型分布。

### 14.4 社区页面

测试点：

1. 帖子列表加载正常。
2. 话题筛选正常。
3. 排序切换正常。
4. 帖子详情正常。
5. 评论、点赞、收藏操作正常。

### 14.5 管理端页面

测试点：

1. 表格数据加载正常。
2. 搜索和筛选正常。
3. 状态标签显示正常。
4. 审核、删除、禁用等操作有确认提示。

## 15. 浏览器调试检查

### 15.1 LocalStorage 检查

登录成功后，浏览器 DevTools -> Application -> LocalStorage 中应存在：

```text
loginUser
token
```

检查点：

1. loginUser 中不包含 password。
2. token 是 JWT 字符串。

### 15.2 Network 检查

登录后请求受保护接口时，在 Network 中查看 Request Headers。

检查点：

1. 请求头中存在 token。
2. 请求路径正确。
3. 响应 code 为 200。
4. 响应 data 符合预期。

### 15.3 Console 检查

检查浏览器 Console 中是否存在明显报错。

## 16. 异常测试

### 16.1 后端服务未启动

| 操作 | 预期结果 |
| --- | --- |
| 前端请求接口 | 提示请求失败或服务不可用 |

### 16.2 数据库连接失败

| 操作 | 预期结果 |
| --- | --- |
| 调用需要数据库的接口 | 返回服务异常，请稍后再试 |

### 16.3 JSON 格式错误

| 操作 | 预期结果 |
| --- | --- |
| Apifox 发送非法 JSON | 返回 Bad Request 或统一错误提示 |

### 16.4 token 过期

| 操作 | 预期结果 |
| --- | --- |
| 使用过期 token 请求接口 | 提示登录失效，请重新登录 |

## 17. 验收标准

当前阶段验收标准：

1. 用户可以成功注册。
2. 用户可以成功登录。
3. 登录成功后 localStorage 存在 loginUser 和 token。
4. loginUser 中不包含 password。
5. 请求心情记录接口时请求头自动携带 token。
6. 用户可以新增心情记录。
7. 用户可以查询自己的心情记录。
8. 用户可以删除自己的心情记录。
9. 不传 userId 也能查询当前用户记录。
10. 退出登录后 token 被清除。
11. 未登录访问首页或受保护接口会被拦截或提示。
12. 接口返回结构统一为 Result。
13. 系统异常能被全局异常处理捕获。
14. 新增和修改心情记录可以生成 AI 回复。
15. AI 回复可以在列表和详情中展示。
16. AI 调用记录可以保存到 ai_reply 表。
17. AI 调用失败时记录流程不中断。
18. 最近 7 天心情趋势展示正常。
19. 前端主要操作具备 message、loading、empty 状态。

后续完整系统验收标准：

1. 用户可以发布匿名帖子。
2. 用户可以评论、点赞和收藏帖子。
3. 管理员可以管理用户、帖子、评论和话题。
4. 管理员可以查看 AI 回复记录。
5. 用户端更完整的情绪统计图表展示正常。
6. 管理端平台统计展示正常。
7. 分页和条件查询正常。
8. 权限控制有效。
9. 项目可以按部署文档正常运行。

## 18. 测试结论

本系统测试需要覆盖功能正确性、接口规范性、权限安全性、异常稳定性和前后端联调完整性。当前阶段重点验证注册登录、JWT、密码加密、心情记录、AI 回复、AI 调用记录、用户端趋势统计和统一返回等用户端闭环功能；后续阶段再逐步覆盖匿名社区、后台管理和更完整的数据统计。

通过以上测试，可以保证系统核心流程可运行、关键权限不被绕过、数据返回结构统一，并为后续功能扩展提供稳定基础。
